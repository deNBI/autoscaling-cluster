# The content of this file (ansible_hosts) should be generated by BiBiGrid during instance initialization.
# Depending on the used base image the ansible_user must be changed. The local ip address of each worker must
# be inserted in the worker section

[master]
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3
